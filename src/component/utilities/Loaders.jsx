import { MutatingDots } from "react-loader-spinner";

const MainLoader = ({ corlor }) => {
    <MutatingDots
        height="100"
        width="100"
        color="#ffffff"
        secondaryColor='#ffffff'
        radius='12.5'
        ariaLabel="mutating-dots-loading"
        wrapperStyle={{}}
        wrapperClass=""
        visible={true}
    />
}

export default MainLoader;
